<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Detail</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <nav>
    <a href="/home">Home</a>
    <a href="/cars">Cars</a>
    <a href="/about">About</a>
    <a href="/contact">Contact</a>
  </nav>
  <h1><%= car.name %></h1>
  <img src="<%= car.imageUrl %>" alt="<%= car.name %>">
  <p><%= car.description %></p>
  <p>Age: <%= car.age %></p>
  <p>Fuel Type: <%= car.fuelType %></p>
  <p>Features: <%= car.features.join(', ') %></p>
  <p>Manufacturer: <%= car.manufacturer.name %></p>
  <a href="/detail/<%= car.manufacturer.id %>">Manufacturer Details</a>
</body>
</html>
